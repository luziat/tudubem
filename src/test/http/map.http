@baseUrl = http://localhost:8080
@imagePath = ./hospital_refined_map.png

### 1) CREATE MAP
POST {{baseUrl}}/map
Content-Type: application/json

{
  "name": "Earth"
}

> {%
  client.global.set("mapId", response.body.id);
%}

### 2) READ ALL
GET {{baseUrl}}/map
Accept: application/json

### 3) READ ONE
GET {{baseUrl}}/map/{{mapId}}
Accept: application/json

### 4) UPDATE NAME
PUT {{baseUrl}}/map/{{mapId}}
Content-Type: application/json

{
  "name": "Earth Updated"
}

### 5) UPLOAD SENSOR MAP (multipart)
# NOTE: {{imagePath}} 파일이 실제로 존재해야 합니다.
POST {{baseUrl}}/map/{{mapId}}/sensor-map
Content-Type: multipart/form-data; boundary=MapBoundary

--MapBoundary
Content-Disposition: form-data; name="file"; filename="hospital_refined_map.png"
Content-Type: image/png

< ./hospital_refined_map.png
--MapBoundary--

### 6) READ ONE (CHECK sensorMapImagePath)
GET {{baseUrl}}/map/{{mapId}}
Accept: application/json

### 7) READ SENSOR MAP IMAGE
GET {{baseUrl}}/map/{{mapId}}/sensor-map
Accept: image/*

### 8) DELETE
DELETE {{baseUrl}}/map/{{mapId}}

### 9) READ DELETED (EXPECT 404)
GET {{baseUrl}}/map/{{mapId}}
Accept: application/json
