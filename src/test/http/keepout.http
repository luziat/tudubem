@baseUrl = http://localhost:8080

### 1) CREATE MAP
POST {{baseUrl}}/map
Content-Type: application/json

{
  "name": "Keepout Test Map"
}

> {%
  client.global.set("mapId", response.body.id);
%}

### 2) CREATE KEEPOUT ZONE
POST {{baseUrl}}/map/{{mapId}}/keepout-zones
Content-Type: application/json

{
  "name": "No-Go Zone A",
  "verticesJson": "[[20,20],[200,20],[200,120],[20,120]]",
  "enabled": true,
  "reason": "고정 장애물 구역"
}

> {%
  client.global.set("keepoutId", response.body.id);
%}

### 3) READ KEEPOUT ZONES
GET {{baseUrl}}/map/{{mapId}}/keepout-zones
Accept: application/json

### 4) READ KEEPOUT ZONE ONE
GET {{baseUrl}}/map/{{mapId}}/keepout-zones/{{keepoutId}}
Accept: application/json

### 5) UPDATE KEEPOUT ZONE
PUT {{baseUrl}}/map/{{mapId}}/keepout-zones/{{keepoutId}}
Content-Type: application/json

{
  "name": "No-Go Zone A Updated",
  "verticesJson": "[[30,30],[220,30],[220,130],[30,130]]",
  "enabled": false,
  "reason": "일시 해제 테스트"
}

### 6) READ UPDATED KEEPOUT ZONE
GET {{baseUrl}}/map/{{mapId}}/keepout-zones/{{keepoutId}}
Accept: application/json

### 7) DELETE KEEPOUT ZONE
DELETE {{baseUrl}}/map/{{mapId}}/keepout-zones/{{keepoutId}}

### 8) READ DELETED KEEPOUT ZONE (EXPECT 404)
GET {{baseUrl}}/map/{{mapId}}/keepout-zones/{{keepoutId}}
Accept: application/json

### 9) DELETE MAP
DELETE {{baseUrl}}/map/{{mapId}}
